{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities of the main window. The frontend talks to the Rust core through commands only; no direct SQL, no arbitrary shell.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:allow-set-theme",
    "store:default",
    "player:default"
  ]
}
